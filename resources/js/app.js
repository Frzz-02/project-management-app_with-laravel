import './bootstrap';
import Alpine from 'alpinejs';

// Mendaftarkan Alpine.js ke global window agar bisa digunakan di seluruh aplikasi
window.Alpine = Alpine;

// Memulai Alpine.js - ini akan mengaktifkan semua fitur Alpine di halaman
Alpine.start();
